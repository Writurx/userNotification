<%= form_with(model: notification, local: true) do |form| %>
  <% if notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notification.errors.count, "error") %> prohibited this notification from being saved:</h2>

      <ul>
      <% notification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sms %>
    <%= form.check_box :sms, id: :notification_sms %>
  </div>

  <div class="field">
    <%= form.label :textCode %>
    <%= form.text_field :textCode, id: :notification_textCode %>
  </div>

  <div class="field">
    <%= form.label :shablon %>
    <%= form.text_area :shablon, id: :notification_shablon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
